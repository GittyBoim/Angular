import { PictureSizeDirective } from './picture-size.directive';

describe('PictureSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new PictureSizeDirective();
    expect(directive).toBeTruthy();
  });
});
